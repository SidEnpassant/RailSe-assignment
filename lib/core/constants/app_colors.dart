import 'package:flutter/material.dart';

class AppColors {
  // Background colors
  static const Color backgroundColor = Color(0xFFF8F9FA);
  static const Color cardBackground = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF2D3748);
  static const Color textSecondary = Color(0xFF718096);
  static const Color textMuted = Color(0xFF9CA3AF);

  // Status colors - Not Started (Blue theme)
  static const Color notStartedPrimary = Color(0xFF3B82F6);
  static const Color notStartedBackground = Color(0xFFEBF4FF);
  static const Color notStartedText = Color(0xFF1E40AF);

  // Status colors - Started/In Progress (Orange/Yellow theme)
  static const Color startedPrimary = Color(0xFFF59E0B);
  static const Color startedBackground = Color(0xFFFEF3C7);
  static const Color startedText = Color(0xFFD97706);

  // Status colors - Completed (Green theme)
  static const Color completedPrimary = Color(0xFF10B981);
  static const Color completedBackground = Color(0xFFD1FAE5);
  static const Color completedText = Color(0xFF059669);

  // Status colors - Overdue (Red theme)
  static const Color overduePrimary = Color(0xFFEF4444);
  static const Color overdueBackground = Color(0xFFFEE2E2);
  static const Color overdueText = Color(0xFFDC2626);

  // High priority indicator
  static const Color highPriority = Color(0xFFDC2626);

  // Button colors
  static const Color buttonPrimary = Color(0xFF3B82F6);
  static const Color buttonSuccess = Color(0xFF10B981);

  // Border colors
  static const Color borderLight = Color(0xFFE5E7EB);
  static const Color borderMedium = Color(0xFFD1D5DB);
}
